{
  "version": 2,
  "buildCommand": "npm run build -- --configuration=production",
  "outputDirectory": "dist/frontend",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
